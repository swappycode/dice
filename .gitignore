# Rust
/target/
# Desktop client (own workspace)
apps/desktop-client/src-tauri/target/
apps/desktop-client/src-tauri/gen/
apps/desktop-client/dist/
**/node_modules/
# cargo-fuzz generated artifacts (the targets + Cargo.toml ARE committed)
/fuzz/target/
/fuzz/corpus/
/fuzz/artifacts/
/fuzz/coverage/

# Local environment / secrets
.env
*.local

# Dev runtime artifacts (auto-generated by the monolith in dev profiles)
/dev/
# Local-fs media store (DICE_MEDIA_DIR default; attachments written at runtime)
/data/

# Local QA / testing scratch — ad-hoc launcher scripts + run logs, machine-specific
/qa/

# Context-only docs kept LOCAL (never published): planning, testing, handoff, and
# meta notes that exist only to give the user + assistant context — not product docs.
/docs/ROADMAP.md
/docs/HANDOFF.md
/docs/git-notes.md
/docs/testing-*.md
/docs/design/m2-theme-pack.md

# OS noise
.DS_Store
Thumbs.db

# NOTE: .sqlx/ and Cargo.lock are COMMITTED — never add them here
/client-build.log
/monolith-run.log
